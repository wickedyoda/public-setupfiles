# server_config/ubuntu

## Overview
Ubuntu-specific server bootstrap and Docker setup scripts.

## Contents
- `ubuntu-docker-install.sh`
- `ubuntu-setup.sh`

## Usage
Use these scripts when preparing Ubuntu servers, especially where Docker is part of the baseline.

## Notes
Review package repositories, Docker prerequisites, and host networking assumptions before running them.
